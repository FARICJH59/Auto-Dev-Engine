# AgenticFusionPro

AgenticFusionPro full repository scaffold with sample agent, gateway, CLI, dashboard, and workflows.


Requirements:
- After cloning run npm install
- Set VERCEL_TOKEN in GitHub secrets to enable deploy workflow

